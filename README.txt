dolmen.location
***************

``dolmen.location`` provides a couple of utilities and adapters to compute
URLs for locatable objects, in a complete tree (root being a
``cromlech.io.IPublicationRoot`` object), using the
``zope.location.ILocation`` interface.
